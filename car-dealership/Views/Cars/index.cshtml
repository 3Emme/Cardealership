@using Dealership.Models;

<h1>Cars List</h1>
@if (Model.Count == 0)
{
  <p>There are no Cars in the list.</p>
}
<ul>
  @foreach (Car myCar in Model)
  {
    <li>@myCar.MakeModel</li>
    <li>@myCar.Miles</li>
    <li>@myCar.Price</li>
  }
  </ul>
 
